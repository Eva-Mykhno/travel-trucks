import s from "./CamperCard.module.css";

const CamperCard = () => {
  const Camper = {
    id,
    name,
    price,
    rating,
    location,
    description,
    isSingleCamper,
  };

  return <div className={s.wrapper}>k;lk;k</div>;
};

export default CamperCard;
